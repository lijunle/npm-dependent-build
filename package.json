{
  "name": "dependent-build",
  "description": "Trigger dependent build by configuration.",
  "version": "0.0.1",
  "bin": {
    "dependent-build": "./bin.js"
  },
  "scripts": {
    "lint": "eslint .",
    "test": "node ./bin.js"
  },
  "dependencies": {
    "js-yaml": "^3.7.0"
  },
  "devDependencies": {
    "eslint": "^3.11.0",
    "eslint-config-airbnb-base": "^10.0.1",
    "eslint-plugin-import": "^2.2.0"
  }
}
